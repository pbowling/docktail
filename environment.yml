name: docktail
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  # Core scientific stack
  - numpy>=1.21
  - scipy>=1.7
  - pandas>=1.3
  # Chemistry
  - rdkit>=2022.09
  # CLI / config
  - pyyaml>=5.4
  - click>=8.0
  - pip:
      # Install docktail itself in editable mode
      - -e /home/pbowling/docking/docktail
